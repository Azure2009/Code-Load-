services:

  backend:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: 0920juancarlo
      DB_NAME: mydatabase
      


  db:
    image: mysql:9.3
    environment:
      MYSQL_ROOT_PASSWORD: 0920juancarlo
      MYSQL_DATABASE: mydatabase
    volumes:
      - mysql_data:/var/lib/mysql



volumes:
  mysql_data: